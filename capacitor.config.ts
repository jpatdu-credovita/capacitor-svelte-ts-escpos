import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.credovita.capacitorsvelteescpos',
  appName: 'capacitor-svelte-escpos',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
